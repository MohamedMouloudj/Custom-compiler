VAR_GLOBAL {
   INTEGER A,B,C;
   CHAR Z[5],R[1],D[10];
}
DECLARATION {
    CONST INTEGER Y = 5;
    CONST INTEGER X = 4;
}

INSTRUCTION {
    IF (X > (A+B) ){
        A = 1+4-5;
    } ELSE {
        A = 2;
    }
}